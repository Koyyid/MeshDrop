/**
 * Local default: an empty value makes the browser use the current origin.
 * The GitHub Pages workflow replaces this file with the repository's
 * MESHDROP_SIGNALING_URL variable during deployment.
 */
globalThis.MESHDROP_CONFIG = Object.freeze({
  signalingUrl: ""
});
